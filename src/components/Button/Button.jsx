import { LoadButton } from './Button.styled';

export const Button = () => {
  return (
    <LoadButton
      type="button"
      //   disabled="false"
    >
      Load more
    </LoadButton>
  );
};
